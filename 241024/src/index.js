var login = function (user) {
    if ("kickCount" in user) {
        // user의 타입에 kickCount 키를 가지고 있다면 Admin
        console.log("".concat(user.name, "\uC740 \uAD00\uB9AC\uC790\uC785\uB2C8\uB2E4."));
    }
};
